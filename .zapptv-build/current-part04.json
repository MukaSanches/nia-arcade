[{"path":"app/src/main/java/com/mukasanches/zapptv/model/ProgramInfo.kt","content":"package com.mukasanches.zapptv.model\n\ndata class ProgramInfo(\n    val channelId: String,\n    val title: String,\n    val subtitle: String? = null,\n    val description: String? = null,\n    val startMillis: Long? = null,\n    val endMillis: Long? = null,\n    val nextTitle: String? = null,\n    val nextStartMillis: Long? = null,\n    val nextEndMillis: Long? = null\n) {\n    fun progress(now: Long = System.currentTimeMillis()): Float {\n        val start = startMillis ?: return 0f\n        val end = endMillis ?: return 0f\n        if (end <= start) return 0f\n        return ((now - start).toFloat() / (end - start).toFloat()).coerceIn(0f, 1f)\n    }\n}\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/model/ProgramSlot.kt","content":"package com.mukasanches.zapptv.model\n\ndata class ProgramSlot(\n    val channelId: String,\n    val title: String,\n    val subtitle: String? = null,\n    val description: String? = null,\n    val startMillis: Long,\n    val endMillis: Long\n) {\n    fun isLive(now: Long = System.currentTimeMillis()): Boolean =\n        now >= startMillis && now < endMillis\n}\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/model/TvCapabilityProfile.kt","content":"package com.mukasanches.zapptv.model\n\ndata class TvCapabilityProfile(\n    val manufacturer: String,\n    val model: String,\n    val androidApi: Int,\n    val fingerprint: String,\n    val isTcl: Boolean,\n    val tvInputs: List<TvInputSummary>,\n    val tunerCount: Int,\n    val hdmiCount: Int,\n    val nativeTvPackage: String?\n)\n\ndata class TvInputSummary(\n    val id: String,\n    val label: String,\n    val type: Int,\n    val state: Int,\n    val packageName: String?,\n    val passthrough: Boolean\n)\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/nativebridge/GenericTvAdapter.kt","content":"package com.mukasanches.zapptv.nativebridge\n\nimport android.content.Context\nimport android.media.tv.TvInputInfo\nimport android.media.tv.TvInputManager\nimport android.os.Build\nimport com.mukasanches.zapptv.model.TvCapabilityProfile\nimport com.mukasanches.zapptv.model.TvInputSummary\n\nopen class GenericTvAdapter : TvPlatformAdapter {\n    override fun detect(context: Context): TvCapabilityProfile {\n        val manager = context.getSystemService(TvInputManager::class.java)\n        val inputList: List<TvInputInfo> = manager?.tvInputList ?: emptyList()\n\n        val inputs = inputList.map { input: TvInputInfo ->\n            val packageName = runCatching {\n                input.serviceInfo.packageName\n            }.getOrNull()\n\n            TvInputSummary(\n                id = input.id,\n                label = runCatching { input.loadLabel(context).toString() }\n                    .getOrDefault(\"Entrada de TV\"),\n                type = input.type,\n                state = runCatching { manager?.getInputState(input.id) }\n                    .getOrNull() ?: TvInputManager.INPUT_STATE_DISCONNECTED,\n                packageName = packageName,\n                passthrough = input.isPassthroughInput\n            )\n        }\n\n        val tunerInputs = inputs.filter { it.type == TvInputInfo.TYPE_TUNER }\n        return TvCapabilityProfile(\n            manufacturer = Build.MANUFACTURER.orEmpty(),\n            model = Build.MODEL.orEmpty(),\n            androidApi = Build.VERSION.SDK_INT,\n            fingerprint = Build.FINGERPRINT.orEmpty(),\n            isTcl = Build.MANUFACTURER.equals(\"TCL\", ignoreCase = true),\n            tvInputs = inputs,\n            tunerCount = tunerInputs.size,\n            hdmiCount = inputs.count { it.type == TvInputInfo.TYPE_HDMI },\n            nativeTvPackage = tunerInputs.firstNotNullOfOrNull { it.packageName }\n        )\n    }\n}\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/nativebridge/NativeChannelReader.kt","content":"package com.mukasanches.zapptv.nativebridge\n\nimport android.content.Context\nimport android.media.tv.TvContract\nimport com.mukasanches.zapptv.data.ChannelResolver\nimport com.mukasanches.zapptv.model.Channel\nimport com.mukasanches.zapptv.model.ChannelSource\nimport com.mukasanches.zapptv.model.ProgramInfo\n\nclass NativeChannelReader(private val context: Context) {\n\n    fun readChannels(tunerInputIds: Set<String>): List<Channel> {\n        if (tunerInputIds.isEmpty()) return emptyList()\n\n        val projection = arrayOf(\n            TvContract.Channels._ID,\n            TvContract.Channels.COLUMN_DISPLAY_NUMBER,\n            TvContract.Channels.COLUMN_DISPLAY_NAME,\n            TvContract.Channels.COLUMN_INPUT_ID,\n            TvContract.Channels.COLUMN_BROWSABLE\n        )\n\n        return runCatching {\n            val channels = mutableListOf<Channel>()\n            context.contentResolver.query(\n                TvContract.Channels.CONTENT_URI,\n                projection,\n                null,\n                null,\n                null\n            )?.use { cursor ->\n                val idIndex = cursor.getColumnIndexOrThrow(TvContract.Channels._ID)\n                val numberIndex = cursor.getColumnIndexOrThrow(TvContract.Channels.COLUMN_DISPLAY_NUMBER)\n                val nameIndex = cursor.getColumnIndexOrThrow(TvContract.Channels.COLUMN_DISPLAY_NAME)\n                val inputIndex = cursor.getColumnIndexOrThrow(TvContract.Channels.COLUMN_INPUT_ID)\n                val browsableIndex = cursor.getColumnIndexOrThrow(TvContract.Channels.COLUMN_BROWSABLE)\n\n                while (cursor.moveToNext()) {\n                    val inputId = cursor.getString(inputIndex) ?: continue\n                    if (inputId !in tunerInputIds) continue\n                    if (cursor.getInt(browsableIndex) == 0) continue\n\n                    val nativeId = cursor.getLong(idIndex)\n                    val name = cursor.getString(nameIndex)?.ifBlank { null } ?: \"Canal\"\n                    val number = cursor.getString(numberIndex).orEmpty()\n\n                    channels += Channel(\n                        id = \"native:$nativeId\",\n                        number = number,\n                        name = name,\n                        source = ChannelSource.NATIVE_TUNER,\n                        canonicalId = ChannelResolver.canonicalId(name),\n                        category = \"TV aberta\",\n                        uri = TvContract.buildChannelUri(nativeId).toString(),\n                        nativeInputId = inputId,\n                        nativeChannelId = nativeId\n                    )\n                }\n            }\n            channels.sortedWith(compareBy({ channelNumberKey(it.number) }, { it.name }))\n        }.getOrElse { emptyList() }\n    }\n\n    fun readCurrentPrograms(channels: List<Channel>): Map<String, ProgramInfo> {\n        val byNativeId = channels\n            .filter { it.nativeChannelId != null }\n            .associateBy { it.nativeChannelId!! }\n        if (byNativeId.isEmpty()) return emptyMap()\n\n        val now = System.currentTimeMillis()\n        val projection = arrayOf(\n            TvContract.Programs.COLUMN_CHANNEL_ID,\n            TvContract.Programs.COLUMN_TITLE,\n            TvContract.Programs.COLUMN_EPISODE_TITLE,\n            TvContract.Programs.COLUMN_SHORT_DESCRIPTION,\n            TvContract.Programs.COLUMN_START_TIME_UTC_MILLIS,\n            TvContract.Programs.COLUMN_END_TIME_UTC_MILLIS\n        )\n\n        return runCatching {\n            val result = mutableMapOf<String, ProgramInfo>()\n            val selection =\n                TvContract.Programs.COLUMN_START_TIME_UTC_MILLIS + \" <= ? AND \" +\n                    TvContract.Programs.COLUMN_END_TIME_UTC_MILLIS + \" > ?\"\n\n            context.contentResolver.query(\n                TvContract.Programs.CONTENT_URI,\n                projection,\n                selection,\n                arrayOf(now.toString(), now.toString()),\n                null\n            )?.use { cursor ->\n                val channelIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_CHANNEL_ID)\n                val titleIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_TITLE)\n                val episodeIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_EPISODE_TITLE)\n                val descriptionIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_SHORT_DESCRIPTION)\n                val startIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_START_TIME_UTC_MILLIS)\n                val endIndex = cursor.getColumnIndexOrThrow(TvContract.Programs.COLUMN_END_TIME_UTC_MILLIS)\n\n                while (cursor.moveToNext()) {\n                    val nativeId = cursor.getLong(channelIndex)\n                    val channel = byNativeId[nativeId] ?: continue\n                    result[channel.id] = ProgramInfo(\n                        channelId = channel.id,\n                        title = cursor.getString(titleIndex)?.ifBlank { null } ?: \"Ao vivo\",\n                        subtitle = cursor.getString(episodeIndex),\n                        description = cursor.getString(descriptionIndex),\n                        startMillis = cursor.getLong(startIndex),\n                        endMillis = cursor.getLong(endIndex)\n                    )\n                }\n            }\n            result\n        }.getOrElse { emptyMap() }\n    }\n\n    private fun channelNumberKey(number: String): Double {\n        val normalized = number.replace(',', '.')\n        return normalized.toDoubleOrNull()\n            ?: Regex(\"\\\\d+(?:\\\\.\\\\d+)?\").find(normalized)?.value?.toDoubleOrNull()\n            ?: Double.MAX_VALUE\n    }\n}\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/nativebridge/NativeTvBridge.kt","content":"package com.mukasanches.zapptv.nativebridge\n\nimport android.content.Context\nimport android.content.Intent\nimport android.media.tv.TvContract\nimport android.net.Uri\nimport com.mukasanches.zapptv.model.Channel\nimport com.mukasanches.zapptv.model.TvCapabilityProfile\nimport com.mukasanches.zapptv.model.TvInputSummary\n\nclass NativeTvBridge(private val context: Context) {\n\n    fun launchNativeTv(profile: TvCapabilityProfile): Boolean {\n        val packageName = profile.nativeTvPackage\n        if (!packageName.isNullOrBlank()) {\n            val packageManager = context.packageManager\n            val launch = packageManager.getLeanbackLaunchIntentForPackage(packageName)\n                ?: packageManager.getLaunchIntentForPackage(packageName)\n            if (launch != null && start(launch)) return true\n        }\n\n        return start(Intent(Intent.ACTION_VIEW, TvContract.Channels.CONTENT_URI))\n    }\n\n    fun launchChannel(channel: Channel): Boolean {\n        val uri = channel.uri?.let(Uri::parse) ?: return false\n        return start(Intent(Intent.ACTION_VIEW, uri))\n    }\n\n    fun launchInput(input: TvInputSummary): Boolean {\n        if (!input.passthrough) return false\n        val uri = TvContract.buildChannelUriForPassthroughInput(input.id)\n        return start(Intent(Intent.ACTION_VIEW, uri))\n    }\n\n    fun launchWeb(url: String): Boolean =\n        start(Intent(Intent.ACTION_VIEW, Uri.parse(url)).addCategory(Intent.CATEGORY_BROWSABLE))\n\n    private fun start(intent: Intent): Boolean {\n        return runCatching {\n            if (context !is android.app.Activity) {\n                intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)\n            }\n            context.startActivity(intent)\n            true\n        }.getOrDefault(false)\n    }\n}\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/nativebridge/TclTvAdapter.kt","content":"package com.mukasanches.zapptv.nativebridge\n\n/**\n * TCL-specific boundary.\n *\n * V1 intentionally starts from public Android TV APIs. Any model-specific behavior must\n * be proven on hardware before it is added here, keeping the rest of the app OEM-agnostic.\n */\nclass TclTvAdapter : GenericTvAdapter()\n"},{"path":"app/src/main/java/com/mukasanches/zapptv/nativebridge/TvAdapterFactory.kt","content":"package com.mukasanches.zapptv.nativebridge\n\nimport android.os.Build\n\nobject TvAdapterFactory {\n    fun create(): TvPlatformAdapter =\n        if (Build.MANUFACTURER.equals(\"TCL\", ignoreCase = true)) TclTvAdapter()\n        else GenericTvAdapter()\n}\n"}]